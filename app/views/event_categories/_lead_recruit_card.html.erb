<div class="card-lead mt-2">
  <div class="card-lead-contact">
    <div class="card-lead-contact-data">
      <i class="fas fa-calendar-day"></i>  <b><%= recruitment.publication_date%></b> //
      <i class="fas fa-user-alt"></i>  <%= recruitment.employer %> //
      <i class="fas fa-building"></i>  <%= recruitment.zip_code %>
    </div>
    <div class="card-lead-contact-data">
      <i class="fas fa-phone-square-alt"></i><%= recruitment.employer_phone %>
      <i class="fas fa-envelope-open-text"></i><%= recruitment.employer_email %>
    </div>
  </div>
  <i class="fas fa-file-contract"></i>  <%= recruitment.job_title %> post en </i>  <%= recruitment.contract_type %>
  <%#= recruitment.job_description %>
</div>
